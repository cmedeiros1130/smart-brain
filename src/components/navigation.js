import React from "react";

const Navigation = () => {
  return (
    <nav style={{ display: "flex", justifyContent: "flex-end" }}>
      <p className="sign-out-nav"> Sign out</p>
    </nav>
  );
};

export default Navigation;
